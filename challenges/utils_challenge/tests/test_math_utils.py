from utils.math_utils import add, multiply

def test_add():
    num_one = 1
    num_two = 1
    assert add(num_one,num_two) == 2

def test_multiply():
    num_one = 1
    num_two = 1
    assert multiply(num_one,num_two) == 1